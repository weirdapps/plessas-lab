{
  "name": "playlist-tools",
  "version": "1.0.0",
  "type": "module",
  "description": "CLI tools for managing YouTube playlists via official Google APIs",
  "scripts": {
    "build": "tsc",
    "auth": "tsx cli/auth.ts",
    "playlists": "tsx cli/playlists.ts",
    "sync": "tsx cli/sync.ts"
  },
  "dependencies": {
    "google-auth-library": "^10.5.0",
    "googleapis": "^174.0.0"
  },
  "//overrides": "OAuth2Client has private fields, so two copies of google-auth-library in the tree are structurally incompatible and google.youtube({auth}) fails to typecheck. googleapis-common pins an exact patch, so collapse the tree onto the single version declared above.",
  "overrides": {
    "google-auth-library": "$google-auth-library"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^7.0.2",
    "tsx": "^4.7.0"
  }
}
