[project]
name = "chat-watch"
version = "0.1.0"
description = "Experimental — polls MS Teams chats and posts [Claude]-prefixed replies via LLM gate"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
